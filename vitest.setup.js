import { beforeAll, afterEach, afterAll } from "vitest";
import '@testing-library/jest-dom';

// Если нет сервера, можно оставить только эти заглушки
beforeAll(() => {
  // Настройки перед всеми тестами, если нужно
});

afterEach(() => {
  // Очистка после каждого теста, если нужно
});

afterAll(() => {
  // Очистка после завершения всех тестов
});
